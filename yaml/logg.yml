##создаст файл app.log по указанному пути
logging:
  file:
   name: \\сетевой ресурс/log/app.log
#   Цветным будет подчеркивать
spring.output.ansi.enabled: ALWAYS
#формат вывода логов
logging.pattern.console: '%C{1.} [%-5level] %d{HH:mm:ss} - %msg%n'
